<section class="container py-16" data-reveal>
  <div class="bg-ink-900 text-cream-50 rounded-3xl p-12 text-center">
    <h2 class="font-display text-4xl mb-4">{{ trans("newsletter.title") }}</h2>
    <p class="text-ink-300 mb-8">{{ trans("newsletter.subtitle") }}</p>
    <salla-newsletter class="max-w-md mx-auto"></salla-newsletter>
  </div>
</section>
